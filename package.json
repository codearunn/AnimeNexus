{
  "name": "animenexus-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "bash start.sh",
    "install-backend": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
